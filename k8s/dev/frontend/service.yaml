apiVersion: v1
kind: Service
metadata:
  name: chat-frontend-svc
  namespace: chat-dev
  labels:
    app: chat-frontend
    environment: dev
spec:
  type: NodePort
  selector:
    app: chat-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
      protocol: TCP
