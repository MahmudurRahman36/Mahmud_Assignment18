apiVersion: v1
kind: Service
metadata:
  name: chat-frontend-svc
  namespace: chat-prod
  labels:
    app: chat-frontend
    environment: prod
spec:
  type: NodePort
  selector:
    app: chat-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30020
      protocol: TCP
