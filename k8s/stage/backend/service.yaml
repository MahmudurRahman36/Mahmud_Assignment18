apiVersion: v1
kind: Service
metadata:
  name: chat-backend-svc
  namespace: chat-stage
  labels:
    app: chat-backend
    environment: stage
spec:
  type: ClusterIP
  selector:
    app: chat-backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
